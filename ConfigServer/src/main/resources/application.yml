server:
  port: 8085

spring:
  application:
    name: config-server

  cloud:
    config:
      server:
        git:
          uri: https://github.com/Moinuddin-150204014/ConfigServer
          clone-on-start: true

